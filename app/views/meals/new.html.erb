
<%= simple_form_for current_user.meals.new, url: meals_path do |m| %>
<%= m.input :photo, as: :file, input_html:{}, label: false %>
<%= m.input :good, as: :boolean, label: false, hint: "Good for you? Be honest." %>
<%= m.button :submit, "Let's Eat!", class: "btn btn-primary" %>
<% end %>
